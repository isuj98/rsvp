{
  "name": "johnsean-kristine-wedding-invitation",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx git lfs install && npx git lfs pull && bash ./scripts/vercel-build.sh && vite build",
    "preview": "vite preview",
    "postinstall": "chmod +x node_modules/.bin/* 2>/dev/null || true"
  },
  "dependencies": {
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "@google/genai": "^1.34.0",
    "mongodb": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@vitejs/plugin-react": "^5.0.2",
    "typescript": "^5.9.0",
    "vite": "^6.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
